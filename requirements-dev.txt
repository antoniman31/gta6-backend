# Dépendances des CONTRÔLES seulement, jamais du robot.
#
# requirements.txt est installé par les quatre workflows, dont celui du robot
# qui tourne vingt-quatre à quarante-huit fois par jour. Playwright n'y a donc
# pas sa place : il pèserait sur chaque passage pour ne servir qu'aux
# contrôles rendus, lancés sur les pull requests.
#
# Épinglé à l'exact comme le reste : une version non épinglée est une version
# que rien n'a testée.
playwright==1.63.0
